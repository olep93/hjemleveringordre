"use client";import {useParams,useSearchParams} from "next/navigation";import {useEffect,useState} from "react";import {Box,CalendarDays,FileText,MapPin,Phone} from "lucide-react";
export default function Page(){const p=useParams<{id:string}>();const token=useSearchParams().get("token")??"";const[o,setO]=useState<any>(null);const[e,setE]=useState<string|null>(null);useEffect(()=>{fetch(`/api/pickup/${p.id}?token=${encodeURIComponent(token)}`).then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setO(j.order)}).catch(x=>setE(x.message))},[p.id,token]);if(e)return <main className="pickup-public-page"><div className="error-box">{e}</div></main>;if(!o)return <main className="pickup-public-page">Henter hentepakke …</main>;return <main className="pickup-public-page"><header><div className="obs-bygg-wordmark pickup-logo"><span>OBS</span><span>BYGG</span></div><div><p className="eyebrow">HENTEPAKKE</p><h1>{o.title}</h1></div></header><section className="pickup-public-summary"><div><CalendarDays/><span>Dato</span><strong>{o.pickupDate??"Ikke satt"}</strong></div><div><MapPin/><span>Plassering</span><strong>{o.placement??"Ikke valgt"}{o.locationCode?` – ${o.locationCode}`:""}</strong></div><div><Phone/><span>Telefon</span><strong>{o.phone??"Ikke registrert"}</strong></div></section><section className="modern-card"><h2>Kunde og levering</h2><p><strong>{o.customerName}</strong></p><p>{o.deliveryAddress}</p><p className="pickup-comment"><strong>Kommentar:</strong> {o.comment??"Ingen kommentar"}</p></section><section className="modern-card"><h2>Varelinjer</h2><div className="pickup-public-items">{(o.items??[]).filter((x:any)=>!x.isFreight).map((x:any,i:number)=><div key={x.id??i}><Box size={20}/><span>{x.productName??x.description}</span><strong>{x.quantity} {x.unit}</strong></div>)}</div></section>{o.originalDocumentUrl&&<section className="modern-card"><h2><FileText size={21}/> Original kundeordre</h2><a className="blue-action" href={o.originalDocumentUrl} target="_blank">Åpne originaldokument</a><iframe className="pickup-document-frame" src={o.originalDocumentUrl}/></section>}{(o.photos??[]).length>0&&<section className="modern-card"><h2>Bilder av ferdig plukket ordre</h2><div className="pickup-public-photos">{o.photos.map((x:any)=><a href={x.url} target="_blank" key={x.index}><img src={x.url} alt="Plukkebilde"/></a>)}</div></section>}</main>}
